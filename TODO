* keep an eye on compat.php
* CSRF protection
* meltemi PM
* remove folder rss/
* add CHANGELOG
* fix bug, where filter with zero results fails
* fix layout of pagination (CSS?)

* replace mktime() with strtotime()
* store categories in DB
* simplify CSS (shall we call for themes?)
